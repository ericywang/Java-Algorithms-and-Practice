class Solution {
    public int[] runningSum(int[] nums) {
        int count = 0;
        for(int x = 0; x< nums.length; x++){
            count += nums[x];
            nums[x] = count;
        }
        return nums;
    }
}
